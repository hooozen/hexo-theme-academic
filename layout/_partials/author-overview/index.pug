- const {name, degree, motto, field, institution} = theme.author;
.author-overview-outer
  .author-overview
    .author-face(style={'background-image': `url(${url_for(theme.author.face)})`})
    .author-info
      .author-name= name
      if degree
      .author-degree= degree
      if motto
        .author-motto= motto
      if field
        .author-field= field
      if institution
        .author-institution= institution
    .author-social
      a(href="mailto:" + theme.author.email).social-item.icon-email(alt="email" style={ 'background-image': `url(${url_for(theme.icon)})`})
      a(href=theme.author.googleScholar).social-item.icon-scholar(alt="google scholar" style={ 'background-image': `url(${url_for(theme.icon)})`})
      a(href=theme.author.institutionLink).social-item.icon-institution(alt="institution" style={ 'background-image': `url(${url_for(theme.icon)})`})

